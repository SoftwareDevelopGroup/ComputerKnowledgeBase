---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-23
course: 面向对象程序设计A
tags: ["面向对象", "习题", "期末复习"]
prerequisites: ["2.1 类的定义", "2.2 对象的创建与使用", "2.3 方法重载", "2.4 this关键字"]
source: 面向对象程序设计（本科通用教材，兼顾Java、C++两种主流教学语言）
---

# 习题-第2章 类与对象基础

> [!abstract] 本章重点
> 本章习题覆盖类的定义、对象创建、方法重载、this关键字等核心知识点，题型贴合本科大二期末考纲。

---

## 一、选择题

### 单选题

1. 在Java中，类的关键字是（ ）
   A. class
   B. struct
   C. object
   D. define

2. 下列关于类的描述，错误的是（ ）
   A. 类是对象的模板
   B. 类包含属性和方法
   C. 类可以没有属性
   D. 类必须有main方法

3. 在Java中，创建对象使用的关键字是（ ）
   A. create
   B. new
   C. make
   D. build

4. 下列关于构造方法的描述，错误的是（ ）
   A. 构造方法与类同名
   B. 构造方法没有返回值
   C. 构造方法可以重载
   D. 构造方法必须手动调用

5. 方法重载的条件不包括（ ）
   A. 方法名相同
   B. 参数列表不同
   C. 返回值类型不同
   D. 参数类型不同

6. this关键字的作用不包括（ ）
   A. 引用当前对象
   B. 调用当前对象的其他方法
   C. 调用父类的构造方法
   D. 区分同名的成员变量和局部变量

7. 在C++中，创建对象时会自动调用（ ）
   A. main方法
   B. 构造函数
   C. 析构函数
   D. 静态方法

8. 下列关于对象的描述，正确的是（ ）
   A. 对象是类的模板
   B. 对象可以没有属性
   C. 对象是类的实例
   D. 对象必须有方法

### 多选题

1. 下列关于类和对象的描述，正确的有（ ）
   A. 类是抽象的，对象是具体的
   B. 一个类可以创建多个对象
   C. 对象是类的实例
   D. 类和对象是同一概念

2. 下列关于构造方法的描述，正确的有（ ）
   A. 构造方法用于初始化对象
   B. 每个类至少有一个构造方法
   C. 如果没有定义构造方法，编译器会提供默认构造方法
   D. 构造方法可以有参数

3. 下列关于方法重载的描述，正确的有（ ）
   A. 方法名必须相同
   B. 参数个数必须不同
   C. 参数类型必须不同
   D. 返回值类型可以相同或不同

---

## 二、填空题

1. 类是______，对象是______。

2. 类的组成包括______和______。

3. 构造方法的特点是______、______、______。

4. 方法重载的条件是______和______。

5. this关键字用于______。

6. 在Java中，默认构造方法的特点是______。

7. 对象的创建过程包括______和______。

8. 在C++中，析构函数的作用是______。

---

## 三、简答题

1. 什么是类？什么是对象？它们之间的关系是什么？

2. 什么是构造方法？构造方法有什么特点？

3. 什么是方法重载？方法重载的条件是什么？

4. this关键字有哪些用法？

5. 简述Java和C++在类与对象定义上的区别。

---

## 四、综合应用题

### 题1：代码分析

阅读下列Java代码，写出程序的输出结果：

```java
public class Person {
    private String name;
    
    public Person() {
        name = "Unknown";
        System.out.println("Default constructor");
    }
    
    public Person(String name) {
        this.name = name;
        System.out.println("Constructor with name: " + name);
    }
    
    public void sayHello() {
        System.out.println("Hello, " + name);
    }
}

public class Test {
    public static void main(String[] args) {
        Person p1 = new Person();
        Person p2 = new Person("Alice");
        p1.sayHello();
        p2.sayHello();
    }
}
```

### 题2：代码补全

补全下列Java代码，实现一个计算器类，支持整数和浮点数的加减乘除：

```java
public class Calculator {
    // 整数加法
    public int add(int a, int b) {
        return a + b;
    }
    
    // 浮点数加法
    public double add(double a, double b) {
        return a + b;
    }
    
    // 补全减法方法（整数和浮点数）
    // 补全乘法方法（整数和浮点数）
    // 补全除法方法（整数和浮点数）
}
```

### 题3：程序设计

设计一个矩形类Rectangle，包含：
(1) 私有属性：长(length)、宽(width)
(2) 构造方法：无参构造和带参构造
(3) 方法：计算面积(getArea)、计算周长(getPerimeter)
(4) 使用this关键字

### 题4：C++实现

用C++实现题3中的矩形类。

---

## 参考答案

### 选择题答案

**单选题**：
1. A
2. D
3. B
4. D
5. C
6. C
7. B
8. C

**多选题**：
1. ABC
2. ABCD
3. AD

### 填空题答案

1. 对象的模板，类的实例
2. 属性（成员变量），方法（成员函数）
3. 与类同名，无返回值，创建对象时自动调用
4. 方法名相同，参数列表不同
5. 引用当前对象
6. 无参数，无返回值，不做任何操作
7. 分配内存，调用构造方法
8. 释放对象占用的资源

### 简答题答案

1. 类是对象的模板，定义了对象的属性和行为；对象是类的实例，是具体的实体。关系：对象是类的实例化。

2. 构造方法用于初始化对象。特点：与类同名、无返回值、创建对象时自动调用、可重载。

3. 方法重载是指同一类中多个方法同名但参数列表不同。条件：方法名相同、参数列表不同（个数或类型）。

4. this用法：引用当前对象、调用当前对象的方法、区分同名成员变量和局部变量、调用当前类的其他构造方法。

5. Java用class关键字，C++用class或struct；Java所有方法在类内，C++可在类外定义；Java无析构函数，靠垃圾回收；C++有析构函数。

### 综合应用题答案

**题1答案**：
```
Default constructor
Constructor with name: Alice
Hello, Unknown
Hello, Alice
```

**题2答案**：
```java
public int subtract(int a, int b) {
    return a - b;
}

public double subtract(double a, double b) {
    return a - b;
}

public int multiply(int a, int b) {
    return a * b;
}

public double multiply(double a, double b) {
    return a * b;
}

public int divide(int a, int b) {
    return a / b;
}

public double divide(double a, double b) {
    return a / b;
}
```

**题3答案**：
```java
public class Rectangle {
    private double length;
    private double width;
    
    public Rectangle() {
        this.length = 0;
        this.width = 0;
    }
    
    public Rectangle(double length, double width) {
        this.length = length;
        this.width = width;
    }
    
    public double getArea() {
        return this.length * this.width;
    }
    
    public double getPerimeter() {
        return 2 * (this.length + this.width);
    }
}
```

**题4答案**：
```cpp
#include <iostream>
using namespace std;

class Rectangle {
private:
    double length;
    double width;
public:
    Rectangle() : length(0), width(0) {}
    Rectangle(double l, double w) : length(l), width(w) {}
    
    double getArea() const {
        return length * width;
    }
    
    double getPerimeter() const {
        return 2 * (length + width);
    }
};
```

---

**导航**：上一章习题 [[习题-第1章-面向对象概述]] · [[MOC - 面向对象程序设计A|返回课程地图]] · 下一章习题 [[习题-第3章-封装与访问控制]]