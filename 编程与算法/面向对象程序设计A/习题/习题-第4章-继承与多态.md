---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-23
course: 面向对象程序设计A
tags: ["面向对象", "习题", "期末复习"]
prerequisites: ["4.1 继承的概念", "4.2 方法重写", "4.3 多态的实现", "4.4 super关键字"]
source: 面向对象程序设计（本科通用教材，兼顾Java、C++两种主流教学语言）
---

# 习题-第4章 继承与多态

> [!abstract] 本章重点
> 本章习题覆盖继承概念、方法重写、多态实现、super关键字等核心知识点，题型贴合本科大二期末考纲。

---

## 一、选择题

### 单选题

1. 在Java中，实现继承使用的关键字是（ ）
   A. implements
   B. extends
   C. inherit
   D. derive

2. 下列关于继承的描述，错误的是（ ）
   A. 子类可以继承父类的所有成员
   B. 子类可以添加新的成员
   C. 子类可以重写父类的方法
   D. Java支持多重继承

3. 方法重写的条件不包括（ ）
   A. 方法名相同
   B. 参数列表相同
   C. 返回值类型相同或为子类
   D. 访问修饰符更严格

4. super关键字的作用不包括（ ）
   A. 调用父类的构造方法
   B. 调用父类的成员方法
   C. 访问父类的成员变量
   D. 调用当前类的其他构造方法

5. 多态的实现方式是（ ）
   A. 向上转型+方法重写
   B. 向下转型+方法重载
   C. 向上转型+方法重载
   D. 向下转型+方法重写

6. 下列关于向上转型的描述，正确的是（ ）
   A. 父类引用指向子类对象
   B. 子类引用指向父类对象
   C. 必须进行强制类型转换
   D. 会丢失子类特有的方法

7. 动态绑定发生在（ ）
   A. 编译时
   B. 运行时
   C. 链接时
   D. 加载时

8. 在C++中，实现多态需要使用（ ）
   A. inline关键字
   B. virtual关键字
   C. static关键字
   D. const关键字

### 多选题

1. 下列关于继承的描述，正确的有（ ）
   A. 继承是"is-a"关系
   B. 子类继承父类的非private成员
   C. 子类可以调用父类的构造方法
   D. Java支持单继承，C++支持多重继承

2. 下列关于方法重写的描述，正确的有（ ）
   A. 方法名必须相同
   B. 参数列表必须相同
   C. 返回值类型必须相同
   D. 访问修饰符不能更严格

3. 下列关于多态的描述，正确的有（ ）
   A. 多态提高了代码的灵活性
   B. 多态通过向上转型实现
   C. 多态需要方法重写
   D. 多态分为编译时多态和运行时多态

---

## 二、填空题

1. 继承的语法是______。

2. 子类继承父类的______成员，但不能继承______成员。

3. 方法重写的条件是______、______、______。

4. super关键字用于______。

5. 多态的三个条件是______、______、______。

6. 向上转型是______。

7. 动态绑定是______。

8. 在C++中，虚函数的作用是______。

---

## 三、简答题

1. 什么是继承？继承的优点是什么？

2. 什么是方法重写？方法重写与方法重载有什么区别？

3. super关键字有哪些用法？

4. 什么是多态？多态的实现条件是什么？

5. 简述向上转型和向下转型的区别。

---

## 四、综合应用题

### 题1：代码分析

阅读下列Java代码，写出程序的输出结果：

```java
public class Animal {
    public void makeSound() {
        System.out.println("Animal makes sound");
    }
}

public class Dog extends Animal {
    @Override
    public void makeSound() {
        System.out.println("Dog barks");
    }
    
    public void wagTail() {
        System.out.println("Dog wags tail");
    }
}

public class Cat extends Animal {
    @Override
    public void makeSound() {
        System.out.println("Cat meows");
    }
}

public class Test {
    public static void main(String[] args) {
        Animal a1 = new Animal();
        Animal a2 = new Dog();
        Animal a3 = new Cat();
        Dog d = new Dog();
        
        a1.makeSound();
        a2.makeSound();
        a3.makeSound();
        d.makeSound();
        d.wagTail();
    }
}
```

### 题2：代码补全

补全下列Java代码，实现动物类的继承体系：

```java
public class Animal {
    protected String name;
    
    public Animal(String name) {
        this.name = name;
    }
    
    public void eat() {
        System.out.println(name + " eats");
    }
    
    // 抽象方法：发出声音
    public abstract void makeSound();
}

public class Dog extends Animal {
    public Dog(String name) {
        super(name);
    }
    
    // 重写makeSound方法
}

public class Bird extends Animal {
    public Bird(String name) {
        super(name);
    }
    
    // 重写makeSound方法
    // 添加fly方法
}
```

### 题3：程序设计

设计一个图形类层次结构：
(1) 抽象类Shape：包含抽象方法getArea()、getPerimeter()
(2) 子类Circle：包含半径属性，实现getArea()和getPerimeter()
(3) 子类Rectangle：包含长和宽属性，实现getArea()和getPerimeter()
(4) 测试类：创建不同图形对象，调用方法

### 题4：C++实现

用C++实现题3中的图形类层次结构。

---

## 参考答案

### 选择题答案

**单选题**：
1. B
2. D
3. D
4. D
5. A
6. A
7. B
8. B

**多选题**：
1. ABCD
2. ABD
3. ABCD

### 填空题答案

1. class 子类名 extends 父类名
2. 非private，private
3. 方法名相同、参数列表相同、返回值类型兼容
4. 调用父类的成员
5. 继承、方法重写、向上转型
6. 父类引用指向子类对象
7. 运行时根据实际对象类型调用方法
8. 实现运行时多态

### 简答题答案

1. 继承是子类获得父类属性和方法的机制。优点：代码复用、层次结构清晰、扩展性好。

2. 方法重写是子类重新实现父类的方法。区别：重写在子类中，参数列表相同；重载在同一类中，参数列表不同。

3. super用法：调用父类构造方法、调用父类成员方法、访问父类成员变量。

4. 多态是同一接口有不同实现。条件：继承、方法重写、向上转型。

5. 向上转型：父类引用指向子类对象，自动转换；向下转型：子类引用指向父类对象，需要强制转换。

### 综合应用题答案

**题1答案**：
```
Animal makes sound
Dog barks
Cat meows
Dog barks
Dog wags tail
```

**题2答案**：
```java
@Override
public void makeSound() {
    System.out.println(name + " barks");
}

@Override
public void makeSound() {
    System.out.println(name + " chirps");
}

public void fly() {
    System.out.println(name + " flies");
}
```

**题3答案**：
```java
public abstract class Shape {
    public abstract double getArea();
    public abstract double getPerimeter();
}

public class Circle extends Shape {
    private double radius;
    
    public Circle(double radius) {
        this.radius = radius;
    }
    
    @Override
    public double getArea() {
        return Math.PI * radius * radius;
    }
    
    @Override
    public double getPerimeter() {
        return 2 * Math.PI * radius;
    }
}

public class Rectangle extends Shape {
    private double length;
    private double width;
    
    public Rectangle(double length, double width) {
        this.length = length;
        this.width = width;
    }
    
    @Override
    public double getArea() {
        return length * width;
    }
    
    @Override
    public double getPerimeter() {
        return 2 * (length + width);
    }
}

public class Test {
    public static void main(String[] args) {
        Shape[] shapes = {new Circle(5), new Rectangle(4, 6)};
        for (Shape s : shapes) {
            System.out.println("Area: " + s.getArea());
            System.out.println("Perimeter: " + s.getPerimeter());
        }
    }
}
```

**题4答案**：
```cpp
#include <iostream>
#include <cmath>
using namespace std;

class Shape {
public:
    virtual double getArea() const = 0;
    virtual double getPerimeter() const = 0;
};

class Circle : public Shape {
private:
    double radius;
public:
    Circle(double r) : radius(r) {}
    
    double getArea() const override {
        return M_PI * radius * radius;
    }
    
    double getPerimeter() const override {
        return 2 * M_PI * radius;
    }
};

class Rectangle : public Shape {
private:
    double length, width;
public:
    Rectangle(double l, double w) : length(l), width(w) {}
    
    double getArea() const override {
        return length * width;
    }
    
    double getPerimeter() const override {
        return 2 * (length + width);
    }
};
```

---

**导航**：上一章习题 [[习题-第3章-封装与访问控制]] · [[MOC - 面向对象程序设计A|返回课程地图]] · 下一章习题 [[习题-第5章-抽象类与接口]]