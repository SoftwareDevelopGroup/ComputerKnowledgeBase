---
domain: 编程与算法
type: 概念
status: 整理中
created: 2026-07-26
course: 算法设计与分析
chapter: 4
section: 2
tags: ["算法设计与分析", "贪心算法", "计算机考研"]
prerequisites: ["4.1-贪心算法的基本思想"]
source: 《算法设计与分析》（屈婉玲/王晓东本科通用教材）
---

# 4.2 经典贪心算法

> [!abstract] 核心问题
> 活动选择、哈夫曼编码、Dijkstra算法的贪心策略是什么？如何实现？

## 一、活动选择问题

### 1. 问题描述
给定n个活动，每个活动有开始时间sᵢ和结束时间fᵢ，选择最大的兼容活动子集。

### 2. 贪心策略
每次选择结束时间最早的活动。

### 3. 实现

```python
def activity_selection(activities):
    activities.sort(key=lambda x: x[1])
    selected = [activities[0]]
    last_end = activities[0][1]
    
    for i in range(1, len(activities)):
        start, end = activities[i]
        if start >= last_end:
            selected.append(activities[i])
            last_end = end
    
    return selected
```

### 4. 复杂度分析
- **时间复杂度**：O(nlogn)
- **空间复杂度**：O(n)

## 二、哈夫曼编码（最优前缀码）

### 1. 问题描述
给定字符频率，设计最优前缀码，使得编码总长度最小。

### 2. 贪心策略
每次选择频率最低的两个节点合并。

### 3. 实现

```python
import heapq

def huffman_code(frequencies):
    heap = [[freq, [char, ""]] for char, freq in frequencies.items()]
    heapq.heapify(heap)
    
    while len(heap) > 1:
        lo = heapq.heappop(heap)
        hi = heapq.heappop(heap)
        
        for pair in lo[1:]:
            pair[1] = '0' + pair[1]
        for pair in hi[1:]:
            pair[1] = '1' + pair[1]
        
        heapq.heappush(heap, [lo[0] + hi[0]] + lo[1:] + hi[1:])
    
    return sorted(heapq.heappop(heap)[1:], key=lambda p: (len(p[-1]), p))
```

### 4. 复杂度分析
- **时间复杂度**：O(nlogn)
- **空间复杂度**：O(n)

## 三、Dijkstra算法（单源最短路径）

### 1. 问题描述
给定带权有向图，求从源点到所有其他顶点的最短路径。

### 2. 贪心策略
每次选择距离源点最近的顶点，更新其邻接点的距离。

### 3. 实现

```python
import heapq

def dijkstra(graph, start):
    distances = {node: float('inf') for node in graph}
    distances[start] = 0
    priority_queue = [(0, start)]
    
    while priority_queue:
        current_dist, current_node = heapq.heappop(priority_queue)
        
        if current_dist > distances[current_node]:
            continue
        
        for neighbor, weight in graph[current_node].items():
            distance = current_dist + weight
            if distance < distances[neighbor]:
                distances[neighbor] = distance
                heapq.heappush(priority_queue, (distance, neighbor))
    
    return distances
```

### 4. 复杂度分析
- **时间复杂度**：O((V+E)logV)
- **空间复杂度**：O(V)

### 5. 适用条件
- 边权非负

## 四、三种算法对比

| 算法 | 贪心策略 | 时间复杂度 | 适用条件 |
|-----|---------|-----------|---------|
| **活动选择** | 选择结束时间最早的活动 | O(nlogn) | 活动可排序 |
| **哈夫曼编码** | 合并频率最低的两个节点 | O(nlogn) | 字符频率已知 |
| **Dijkstra** | 选择距离最近的顶点 | O((V+E)logV) | 边权非负 |

---

**导航**：上一节 [[4.1-贪心算法的基本思想.md]] · [[MOC - 第4章.md|返回章节导航]] · 下一节 [[4.3-贪心算法的应用.md]]