---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-26
course: Java程序设计
chapter: 8
tags: ["Java", "面向对象", "编程基础", "习题"]
source: 《Java程序设计》本科通用教材
---

# 习题-第8章-异常处理机制

> [!abstract] 本章习题说明
> 本章习题涵盖异常概述、异常处理、自定义异常等核心知识点。

## 一、选择题

1. 以下哪个不是Checked异常（ ）
   A. IOException
   B. SQLException
   C. NullPointerException
   D. ClassNotFoundException

2. 异常处理的关键字不包括（ ）
   A. try
   B. catch
   C. finally
   D. error

3. finally块（ ）执行
   A. 总是
   B. 有时
   C. 从不
   D. 只有发生异常时

4. throws关键字用于（ ）
   A. 抛出异常
   B. 声明异常
   C. 捕获异常
   D. 处理异常

5. 自定义异常应该继承（ ）
   A. Error
   B. Exception
   C. Throwable
   D. Object

## 二、填空题

1. 异常分为______异常和______异常。

2. try-catch-finally块中，______块总是执行。

3. throw关键字用于______异常。

4. throws关键字用于______异常。

5. 自定义异常通常继承______或______。

## 三、简答题

1. 简述异常的分类。

2. 简述try-catch-finally的执行顺序。

3. 简述throws和throw的区别。

4. 简述自定义异常的定义方法。

5. 简述异常处理的最佳实践。

## 四、编程题

1. 编写一个Java程序，处理除零异常。

2. 编写一个自定义异常，处理年龄输入错误。

3. 编写一个Java程序，读取文件并处理异常。

## 参考答案

### 选择题答案

1. C
2. D
3. A
4. B
5. B

### 填空题答案

1. Checked、Unchecked
2. finally
3. 抛出
4. 声明
5. Exception、RuntimeException

### 编程题参考代码

**第1题**

```java
public class DivideByZero {
    public static void main(String[] args) {
        try {
            int a = 10;
            int b = 0;
            int result = a / b;
            System.out.println(result);
        } catch (ArithmeticException e) {
            System.out.println("除零异常: " + e.getMessage());
        } finally {
            System.out.println("程序结束");
        }
    }
}
```

**第2题**

```java
public class InvalidAgeException extends RuntimeException {
    public InvalidAgeException(String message) {
        super(message);
    }
}

public class AgeValidator {
    public static void validateAge(int age) {
        if (age < 0 || age > 150) {
            throw new InvalidAgeException("无效年龄: " + age);
        }
    }
    
    public static void main(String[] args) {
        try {
            validateAge(200);
        } catch (InvalidAgeException e) {
            System.out.println(e.getMessage());
        }
    }
}
```

**第3题**

```java
import java.io.FileReader;
import java.io.BufferedReader;
import java.io.IOException;

public class ReadFile {
    public static void main(String[] args) {
        try (BufferedReader br = new BufferedReader(new FileReader("test.txt"))) {
            String line;
            while ((line = br.readLine()) != null) {
                System.out.println(line);
            }
        } catch (IOException e) {
            System.out.println("读取失败: " + e.getMessage());
        }
    }
}
```