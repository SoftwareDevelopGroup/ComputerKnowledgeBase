---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-26
course: Java程序设计
chapter: 4
tags: ["Java", "面向对象", "编程基础", "习题"]
source: 《Java程序设计》本科通用教材
---

# 习题-第4章-数组与字符串

> [!abstract] 本章习题说明
> 本章习题涵盖数组、字符串、常用字符串操作等核心知识点。

## 一、选择题

1. 以下哪个是正确的数组声明（ ）
   A. int arr[];
   B. int[] arr;
   C. array int[];
   D. int arr[10];

2. 数组的下标从（ ）开始
   A. 0
   B. 1
   C. -1
   D. 任意

3. String类的特点是（ ）
   A. 可变
   B. 不可变
   C. 可以修改
   D. 可以继承

4. 以下哪个方法用于比较字符串内容（ ）
   A. ==
   B. equals()
   C. compare()
   D. match()

5. StringBuilder和StringBuffer的区别是（ ）
   A. StringBuilder是线程安全的
   B. StringBuffer是线程安全的
   C. 两者完全相同
   D. StringBuilder性能更低

## 二、填空题

1. 数组的长度可以通过______属性获取。

2. 二维数组`int[][] arr = {{1,2},{3,4}}`中，`arr[1][0]`的值是______。

3. String类的`substring(0, 5)`方法返回的是索引从______到______的子串。

4. `split(",")`方法用于______字符串。

5. StringBuilder的`reverse()`方法用于______字符串。

## 三、简答题

1. 简述数组的声明和初始化方式。

2. 简述String类的不可变性。

3. 简述String、StringBuilder、StringBuffer的区别。

4. 简述字符串比较的方法。

5. 简述常用的字符串操作方法。

## 四、编程题

1. 编写一个Java程序，求数组的最大值和最小值。

2. 编写一个Java程序，反转字符串。

3. 编写一个Java程序，统计字符串中每个字符出现的次数。

## 参考答案

### 选择题答案

1. B
2. A
3. B
4. B
5. B

### 填空题答案

1. length
2. 3
3. 0、4
4. 分割
5. 反转

### 编程题参考代码

**第1题**

```java
public class ArrayMaxMin {
    public static void main(String[] args) {
        int[] arr = {3, 1, 4, 1, 5, 9, 2, 6};
        int max = arr[0];
        int min = arr[0];
        for (int num : arr) {
            if (num > max) max = num;
            if (num < min) min = num;
        }
        System.out.println("最大值: " + max);
        System.out.println("最小值: " + min);
    }
}
```

**第2题**

```java
public class ReverseString {
    public static void main(String[] args) {
        String str = "Hello";
        StringBuilder sb = new StringBuilder(str);
        String reversed = sb.reverse().toString();
        System.out.println(reversed);
    }
}
```

**第3题**

```java
public class CharCount {
    public static void main(String[] args) {
        String str = "Hello World";
        int[] count = new int[256];
        for (char c : str.toCharArray()) {
            count[c]++;
        }
        for (int i = 0; i < 256; i++) {
            if (count[i] > 0) {
                System.out.println((char) i + ": " + count[i]);
            }
        }
    }
}
```