---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-26
course: Java程序设计
chapter: 5
tags: ["Java", "面向对象", "编程基础", "习题"]
source: 《Java程序设计》本科通用教材
---

# 习题-第5章-类、对象与封装基础

> [!abstract] 本章习题说明
> 本章习题涵盖类、对象、封装、构造方法等核心知识点。

## 一、选择题

1. 类的成员不包括（ ）
   A. 成员变量
   B. 成员方法
   C. 构造方法
   D. 局部变量

2. 创建对象使用（ ）关键字
   A. class
   B. new
   C. object
   D. instance

3. 以下哪个访问修饰符的作用域最广（ ）
   A. private
   B. default
   C. protected
   D. public

4. 封装的目的是（ ）
   A. 隐藏实现细节
   B. 提高性能
   C. 增加代码量
   D. 方便调试

5. 构造方法的特点是（ ）
   A. 有返回值
   B. 方法名与类名相同
   C. 可以被继承
   D. 可以有多个返回值

## 二、填空题

1. 类是对象的______，对象是类的______。

2. 成员变量的默认值：int类型是______，引用类型是______。

3. 四种访问修饰符按作用域从小到大排列是：______、______、______、______。

4. 构造方法的方法名必须与______相同。

5. this关键字用于引用______。

## 三、简答题

1. 简述类和对象的关系。

2. 简述封装的概念和优点。

3. 简述四种访问修饰符的作用域。

4. 简述构造方法的特点和作用。

5. 简述this关键字的作用。

## 四、编程题

1. 编写一个Person类，包含姓名和年龄属性，以及getter和setter方法。

2. 编写一个Circle类，包含半径属性和计算面积的方法。

3. 编写一个Student类，包含姓名、年龄、学号属性，以及构造方法和toString方法。

## 参考答案

### 选择题答案

1. D
2. B
3. D
4. A
5. B

### 填空题答案

1. 模板、实例
2. 0、null
3. private、default、protected、public
4. 类名
5. 当前对象

### 编程题参考代码

**第1题**

```java
public class Person {
    private String name;
    private int age;
    
    public String getName() {
        return name;
    }
    
    public void setName(String name) {
        this.name = name;
    }
    
    public int getAge() {
        return age;
    }
    
    public void setAge(int age) {
        this.age = age;
    }
}
```

**第2题**

```java
public class Circle {
    private double radius;
    
    public Circle(double radius) {
        this.radius = radius;
    }
    
    public double getArea() {
        return Math.PI * radius * radius;
    }
}
```

**第3题**

```java
public class Student {
    private String name;
    private int age;
    private String id;
    
    public Student(String name, int age, String id) {
        this.name = name;
        this.age = age;
        this.id = id;
    }
    
    @Override
    public String toString() {
        return "Student{name='" + name + "', age=" + age + ", id='" + id + "'}";
    }
}
```