---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-26
course: Java程序设计
chapter: 9
tags: ["Java", "面向对象", "编程基础", "习题"]
source: 《Java程序设计》本科通用教材
---

# 习题-第9章-常用工具类与集合基础

> [!abstract] 本章习题说明
> 本章习题涵盖常用工具类、集合框架、常用集合实现类等核心知识点。

## 一、选择题

1. Object类是（ ）
   A. 所有类的父类
   B. 部分类的父类
   C. 没有子类
   D. 抽象类

2. 自动装箱是指（ ）
   A. 基本类型转包装类
   B. 包装类转基本类型
   C. 字符串转基本类型
   D. 基本类型转字符串

3. ArrayList的底层实现是（ ）
   A. 链表
   B. 数组
   C. 哈希表
   D. 红黑树

4. HashSet的特点是（ ）
   A. 有序、可重复
   B. 无序、不可重复
   C. 有序、不可重复
   D. 无序、可重复

5. HashMap允许（ ）
   A. null键和null值
   B. 仅null键
   C. 仅null值
   D. 不允许null

## 二、填空题

1. Object类的常用方法包括：______、______、______、______。

2. 自动装箱的关键字是______。

3. Collection接口的常用方法包括：______、______、______、______。

4. List是______、______的集合。

5. HashMap的底层实现是______+______/______。

## 三、简答题

1. 简述Object类的常用方法。

2. 简述自动装箱和拆箱。

3. 简述List和Set的区别。

4. 简述ArrayList和LinkedList的区别。

5. 简述HashMap的底层实现原理。

## 四、编程题

1. 编写一个Java程序，使用ArrayList存储学生信息。

2. 编写一个Java程序，使用HashSet去重。

3. 编写一个Java程序，使用HashMap统计单词出现次数。

## 参考答案

### 选择题答案

1. A
2. A
3. B
4. B
5. A

### 填空题答案

1. equals、hashCode、toString、clone
2. 自动（编译器自动处理）
3. add、remove、contains、size
4. 有序、可重复
5. 数组、链表、红黑树

### 编程题参考代码

**第1题**

```java
import java.util.ArrayList;
import java.util.List;

public class StudentList {
    public static void main(String[] args) {
        List<String> students = new ArrayList<>();
        students.add("张三");
        students.add("李四");
        students.add("王五");
        
        for (String student : students) {
            System.out.println(student);
        }
    }
}
```

**第2题**

```java
import java.util.HashSet;
import java.util.Set;

public class RemoveDuplicates {
    public static void main(String[] args) {
        int[] arr = {1, 2, 2, 3, 3, 3, 4, 5, 5};
        Set<Integer> set = new HashSet<>();
        for (int num : arr) {
            set.add(num);
        }
        System.out.println(set);
    }
}
```

**第3题**

```java
import java.util.HashMap;
import java.util.Map;

public class WordCount {
    public static void main(String[] args) {
        String text = "hello world hello java java java";
        String[] words = text.split(" ");
        
        Map<String, Integer> countMap = new HashMap<>();
        for (String word : words) {
            countMap.put(word, countMap.getOrDefault(word, 0) + 1);
        }
        
        for (Map.Entry<String, Integer> entry : countMap.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue());
        }
    }
}
```