---
domain: 编程与算法
type: 习题
status: 整理中
created: 2026-07-26
course: Java程序设计
chapter: 7
tags: ["Java", "面向对象", "编程基础", "习题"]
source: 《Java程序设计》本科通用教材
---

# 习题-第7章-抽象类、接口与内部类

> [!abstract] 本章习题说明
> 本章习题涵盖抽象类、接口、内部类等核心知识点。

## 一、选择题

1. 抽象类（ ）被实例化
   A. 可以
   B. 不可以
   C. 有时可以
   D. 取决于子类

2. 抽象方法（ ）方法体
   A. 有
   B. 没有
   C. 可以有也可以没有
   D. 必须有

3. 接口中的方法默认是（ ）
   A. private abstract
   B. protected abstract
   C. public abstract
   D. default

4. 类实现接口使用（ ）关键字
   A. extends
   B. implements
   C. interface
   D. abstract

5. 以下哪个不是内部类的类型（ ）
   A. 成员内部类
   B. 局部内部类
   C. 匿名内部类
   D. 静态外部类

## 二、填空题

1. 抽象类使用______关键字定义。

2. 接口使用______关键字定义。

3. 接口中的常量默认是______、______、______。

4. 一个类可以实现______个接口。

5. 匿名内部类没有______，只能使用一次。

## 三、简答题

1. 简述抽象类的特点。

2. 简述接口的特点。

3. 简述抽象类和接口的区别。

4. 简述内部类的分类和特点。

5. 简述匿名内部类的使用场景。

## 四、编程题

1. 编写一个抽象类Animal和接口Flyable，实现Bird类。

2. 编写一个接口Comparable，实现比较功能。

3. 编写一个Outer类，包含成员内部类和局部内部类。

## 参考答案

### 选择题答案

1. B
2. B
3. C
4. B
5. D

### 填空题答案

1. abstract
2. interface
3. public、static、final
4. 多
5. 类名

### 编程题参考代码

**第1题**

```java
public abstract class Animal {
    protected String name;
    
    public Animal(String name) {
        this.name = name;
    }
    
    public abstract void sound();
}

public interface Flyable {
    void fly();
}

public class Bird extends Animal implements Flyable {
    public Bird(String name) {
        super(name);
    }
    
    @Override
    public void sound() {
        System.out.println("Chirp");
    }
    
    @Override
    public void fly() {
        System.out.println(name + " is flying");
    }
}
```

**第2题**

```java
public interface Comparable<T> {
    int compareTo(T other);
}

public class Person implements Comparable<Person> {
    private String name;
    private int age;
    
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
    
    @Override
    public int compareTo(Person other) {
        return this.age - other.age;
    }
}
```

**第3题**

```java
public class Outer {
    private int outerField = 10;
    
    public class Inner {
        public void show() {
            System.out.println("Outer field: " + outerField);
        }
    }
    
    public void method() {
        int localVar = 20;
        
        class LocalInner {
            public void print() {
                System.out.println("Local var: " + localVar);
            }
        }
        
        LocalInner localInner = new LocalInner();
        localInner.print();
    }
}
```