# 2026-08-02 工作日志

## 优化「第01节 导数概念.md」
- 目标文件：`数学基础/高等数学A(1)/第2章 导数与微分/第01节 导数概念.md`
- 对齐同章 `第08节 函数的连续性与间断点.md` 的 Obsidian 原生风格进行重构优化：
  - 补全 Frontmatter（domain/type/course/status/aliases/tags/created/updated，章节字段 chapter/section/order）。
  - 修复缺陷：英文词 "simplest" → "简单"；正文遗留的 `\[ \]` / `\( \)` 数学定界符统一改为 `$$` / `$` 以保证 Obsidian 正确渲染。
  - 用 Callout 重组内容：`[!abstract]` 知识导航、`[!definition]`、`[!example]`、`[!important]`、`[!note]`、`[!warning]`、`[!theorem]`、`[!summary]`、`[!question]` 自测。
  - 增加「相关概念导航」、`快速复习卡` 公式/概念表、`自测问题`（折叠）。
  - 补充内部链接：交叉链接第1章 `第03节 函数的极限`、`第06节 极限存在准则 两个重要极限`、`第08节 函数的连续性与间断点`；同文件标题链接到 2.2/2.3/三/四 等小节。
  - 处理教科书插图引用 `[图2-1]`～`[图2-5]`：加 `[!note]` 说明其为教材原图、本库未收录，并保留文字自包含描述。
  - 幂函数在 x=0 的脚注①转为 `[!note]` Callout。
- 数学内容全部保留，未改动公式结论。已校验无遗留旧定界符、无断链。

## 新增「数学公式排版规范.md」（全局约定）
- 创建项目根目录指导文件 `数学公式排版规范.md`：要求公式紧凑、短公式行内化、连续推导用 `aligned/cases/gather` 合并到单个 `$$` 块、公式块间至多一个空行、禁止靠孤立公式+大段空行撑长篇幅；含反例/正例与自查清单。适用于本库所有知识文件，约束所有 AI。
- 在根 `AGENTS.md` 的「正文与技术表达」LaTeX 条款后追加绑定规则，引用该规范文件，使所有 AI  agent 在新建/改写/重构含公式内容时强制遵守。
- 注：现有笔记（如本日优化的导数概念）大体合规；后续触及旧笔记时按此规范顺手收紧即可，未发起全库重构。

## 优化「第01节 习题.md」（习题 2-1 解答）
- 用户新建了 `第2章 导数与微分/第01节 习题.md`（原题 1134 行，逐行 `$$` + 空行，正是 `数学公式排版规范.md` 要禁止的松散排版）。
- 对齐 Chapter 1 的 习题 笔记风格进行优化：补全 Frontmatter（title/aliases/type:习题/domain:数学基础/course/chapter/section/order/status:已完成/source/tags/created/updated）；新增 `> [!abstract] 知识导航` 链接到 `[[第01节 导数概念]]` 各小节；每题用 `> [!question]` 包题目、`> [!success]` 包结论、`> [!note]` 包补充（如 16(2) 导函数不连续）。
- 应用紧凑规则：转换 `\[ \]`/`\( \)` → `$$`/`$`；短公式行内化；连续推导合并到单个 `aligned`/`cases` 块；删除公式间多余空行。行数 1134 → 400，所有 20 题解答与答案保持不变（已抽查边际成本、f'(1)、x^{2/3} 在 0 不可导、双曲线面积 2a² 等关键结论）。
- 末尾加 `关联知识：[[第01节 导数概念]]`。已校验无遗留旧定界符。
