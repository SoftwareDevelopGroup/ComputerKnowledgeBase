---
domain: 人工智能与前沿技术
type: 概念
status: 整理中
created: 2026-07-29
course: 云计算技术
chapter: 4
section: 1
tags: ["云计算","虚拟化","容器Docker","云原生","负载均衡","云安全","期末复习"]
prerequisites: ["编程与算法/Python程序设计","人工智能与前沿技术/大数据分析技术","计算机系统/计算机网络"]
source: 《云计算技术与应用》本科教材、阿里云官方文档、云计算运维讲义、高校前沿技术课程讲义
---

# 4.1 云架构设计与 OpenStack

> [!abstract] 本节导读
> 本节从云架构的分层设计出发，系统介绍 IaaS、PaaS、SaaS 三层架构的定位与协作关系，深入剖析 OpenStack 云操作系统的核心组件架构、工作机制与部署模式，最后对比 OpenStack 与其他私有云解决方案的差异，帮助读者建立云平台架构设计的整体认知。

## 一、云架构分层设计

### SPI 三层架构

云计算架构遵循 SPI（SaaS-PaaS-IaaS）分层模型，自底向上逐层抽象：

```mermaid
graph TB
    subgraph "SaaS 层（软件即服务）"
        direction TB
        SaaS1["应用服务<br/>→ CRM/ERP/邮箱<br/>→ 用户直接使用"]
        SaaS2["协作平台<br/>→ 钉钉/飞书/Slack<br/>→ 按需使用"]
    end

    subgraph "PaaS 层（平台即服务）"
        direction TB
        PaaS1["开发平台<br/>→ 数据库中间件<br/>→ 微服务框架"]
        PaaS2["运行时环境<br/>→ Java/Python/Node.js<br/>→ 提供运行时"]
    end

    subgraph "IaaS 层（基础设施即服务）"
        direction TB
        IaaS1["计算资源<br/>→ VM/容器<br/>→ CPU/内存"]
        IaaS2["存储资源<br/>→ 块/文件/对象存储"]
        IaaS3["网络资源<br/>→ VPC/负载均衡<br/>→ 弹性IP"]
    end

    subgraph "物理基础设施"
        direction TB
        HW1["物理服务器<br/>→ 计算节点"]
        HW2["存储设备<br/>→ SAN/NAS/分布式存储"]
        HW3["网络设备<br/>→ 交换机/路由器"]
    end

    SaaS --> PaaS
    PaaS --> IaaS
    IaaS --> HW1
    IaaS --> HW2
    IaaS --> HW3

    style SaaS1 fill:#4CAF50,color:#fff
    style SaaS2 fill:#4CAF50,color:#fff
    style PaaS1 fill:#2196F3,color:#fff
    style PaaS2 fill:#2196F3,color:#fff
    style IaaS1 fill:#FF9800,color:#fff
    style IaaS2 fill:#FF9800,color:#fff
    style IaaS3 fill:#FF9800,color:#fff
    style HW1 fill:#795548,color:#fff
    style HW2 fill:#795548,color:#fff
    style HW3 fill:#795548,color:#fff
```

### 各层对比

| 维度 | IaaS | PaaS | SaaS |
|-----|------|------|------|
| **抽象层级** | 基础设施 | 开发运行平台 | 应用服务 |
| **用户掌控** | OS 及以上全部 | 应用及配置 | 仅使用 |
| **维护责任** | 用户管理 OS、中间件 | 用户管理应用 | 供应商全权管理 |
| **灵活性** | 最高 | 中等 | 最低 |
| **典型产品** | AWS EC2、阿里云 ECS | AWS Elastic Beanstalk、阿里云 RDS | AWS WorkMail、钉钉 |
| **计费模式** | 按实例/小时 | 按资源使用量 | 按用户/订阅 |

### 部署架构模式

```mermaid
graph LR
    subgraph "公有云"
        direction TB
        Pub["公有云<br/>→ 多租户共享<br/>→ 按量计费<br/>→ AWS/Azure/阿里云"]
    end

    subgraph "私有云"
        direction TB
        Pri["私有云<br/>→ 企业专用<br/>→ 数据可控<br/>→ OpenStack/VMware"]
    end

    subgraph "混合云"
        direction TB
        Hyb["混合云<br/>→ 公有+私有<br/>→ 灵活调度<br/>→ 多云协同"]
    end

    subgraph "社区云"
        direction TB
        Com["社区云<br/>→ 多组织共享<br/>→ 行业定制<br/>→ 医疗/政务云"]
    end

    Pub --> Hyb
    Pri --> Hyb
    Com --> Hyb

    style Pub fill:#2196F3,color:#fff
    style Pri fill:#4CAF50,color:#fff
    style Hyb fill:#FF9800,color:#fff
    style Com fill:#9C27B0,color:#fff
```

## 二、OpenStack 架构概述

### OpenStack 是什么

> [!definition] OpenStack
> OpenStack 是一个开源云计算平台，提供 IaaS 层的计算、存储、网络等基础设施服务。它由多个相互独立的服务组件组成，通过 API 进行通信，支持 KVM、QEMU、VMware 等多种虚拟化后端。

### 核心组件架构

```mermaid
graph TB
    subgraph "OpenStack 核心服务"
        direction TB
        subgraph "计算服务"
            Nova["Nova<br/>→ 计算服务<br/>→ VM生命周期管理"]
        end

        subgraph "网络服务"
            Neutron["Neutron<br/>→ 网络服务<br/>→ SDN网络管理"]
        end

        subgraph "存储服务"
            Cinder["Cinder<br/>→ 块存储服务"]
            Glance["Glance<br/>→ 镜像服务"]
            Swift["Swift<br/>→ 对象存储服务"]
        end

        subgraph "身份认证"
            Keystone["Keystone<br/>→ 认证授权<br/>→ 服务目录"]
        end

        subgraph "可视化界面"
            Horizon["Horizon<br/>→ Web管理界面"]
        end

        subgraph "编排与告警"
            Heat["Heat<br/>→ 编排服务"]
            Ceilometer["Ceilometer<br/>→ 监控计量"]
        end
    end

    subgraph "共享依赖"
        direction TB
        RabbitMQ["RabbitMQ<br/>→ 消息队列"]
        MySQL["MariaDB/MySQL<br/>→ 数据库"]
        Redis["Redis<br/>→ 缓存"]
    end

    subgraph "虚拟化层"
        direction TB
        KVM["KVM/QEMU<br/>→ Hypervisor"]
        Libvirt["libvirt<br/>→ 虚拟化管理库"]
    end

    Nova --> Libvirt
    Libvirt --> KVM
    Nova --> Neutron
    Nova --> Cinder
    Nova --> Glance
    Nova --> Keystone
    Neutron --> Keystone
    Cinder --> Keystone
    Glance --> Keystone
    Swift --> Keystone
    Horizon --> Keystone
    Horizon --> Nova
    Horizon --> Neutron
    Horizon --> Cinder
    Horizon --> Glance
    Heat --> Nova
    Heat --> Neutron
    Ceilometer --> Nova
    Ceilometer --> Cinder
    Nova --> RabbitMQ
    Neutron --> RabbitMQ
    Cinder --> RabbitMQ
    Nova --> MySQL
    Neutron --> MySQL
    Cinder --> MySQL
    Glance --> MySQL

    style Nova fill:#FF5722,color:#fff
    style Neutron fill:#2196F3,color:#fff
    style Cinder fill:#4CAF50,color:#fff
    style Glance fill:#9C27B0,color:#fff
    style Swift fill:#00BCD4,color:#fff
    style Keystone fill:#FFC107,color:#333
    style Horizon fill:#795548,color:#fff
    style Heat fill:#607D8B,color:#fff
    style Ceilometer fill:#607D8B,color:#fff
    style KVM fill:#9E9E9E,color:#fff
```

### 核心组件详解

| 组件 | 全称 | 职责描述 |
|-----|------|---------|
| **Nova** | Compute | 计算服务，管理虚拟机的创建、调度、启停、迁移等全生命周期 |
| **Neutron** | Networking | 网络服务，提供虚拟网络、子网、路由、安全组、负载均衡等功能 |
| **Cinder** | Block Storage | 块存储服务，为虚拟机提供可挂载的云硬盘 |
| **Glance** | Image Service | 镜像服务，管理 VM 镜像的上传、存储、注册和分发 |
| **Swift** | Object Storage | 对象存储服务，提供大规模、可扩展的对象存储 |
| **Keystone** | Identity Service | 身份认证与授权，管理用户、角色、租户和服务目录 |
| **Horizon** | Dashboard | Web 可视化管理界面，提供图形化操作入口 |
| **Heat** | Orchestration | 编排服务，支持模板化的云资源自动化部署（类似 AWS CloudFormation） |
| **Ceilometer** | Telemetry | 监控与计量服务，采集资源使用数据，支持计费和告警 |

### Nova 子组件架构

```mermaid
graph TB
    subgraph "Nova API 服务"
        NovaAPI["nova-api<br/>→ 接收HTTP API请求<br/>→ 对外暴露REST API"]
    end

    subgraph "Nova Conductor"
        Conductor["nova-conductor<br/>→ 数据库代理<br/>→ 减少DB直接访问"]
    end

    subgraph "Nova Scheduler"
        Scheduler["nova-scheduler<br/>→ 调度器<br/>→ 选择计算节点"]
    end

    subgraph "Nova Compute 服务"
        subgraph "计算节点1"
            Compute1["nova-compute<br/>→ 调用libvirt<br/>→ 管理VM实例"]
        end
        subgraph "计算节点2"
            Compute2["nova-compute<br/>→ 调用libvirt<br/>→ 管理VM实例"]
        end
    end

    subgraph "Nova Placement"
        Placement["nova-placement<br/>→ 资源跟踪<br/>→ 库存管理"]
    end

    User["用户/CLI"] -->|"REST API"| NovaAPI
    NovaAPI -->|"RPC"| Scheduler
    Scheduler -->|"RPC"| Compute1
    Scheduler -->|"RPC"| Compute2
    Compute1 -->|"RPC"| Conductor
    Compute2 -->|"RPC"| Conductor
    Conductor -->|"SQL"| DB[(Database)]
    NovaAPI -->|"RPC"| Conductor
    NovaAPI -->|"RPC"| Placement
    Scheduler -->|"RPC"| Placement
    Compute1 -->|"QEMU/KVM"| VM1[VMs]
    Compute2 -->|"QEMU/KVM"| VM2[VMs]

    style NovaAPI fill:#FF5722,color:#fff
    style Scheduler fill:#2196F3,color:#fff
    style Compute1 fill:#4CAF50,color:#fff
    style Compute2 fill:#4CAF50,color:#fff
    style Conductor fill:#FF9800,color:#fff
    style Placement fill:#9C27B0,color:#fff
```

### 创建虚拟机流程

```mermaid
sequenceDiagram
    participant User as 用户/CLI
    participant API as nova-api
    participant Sch as nova-scheduler
    participant Comp as nova-compute
    participant Glan as Glance
    participant Neut as Neutron
    participant Cind as Cinder
    participant Lib as libvirt/KVM

    User->>API: POST /servers (创建VM请求)
    API->>Glan: GET /images (查询镜像)
    Glan-->>API: 返回镜像信息
    API->>Sch: RPC: select_hosts (选择主机)
    Sch->>Sch: Filter + Weigher 调度
    Sch-->>API: 返回目标计算节点
    API->>Comp: RPC: create_instance
    Comp->>Glan: 下载镜像到本地
    Glan-->>Comp: 返回镜像数据
    Comp->>Neutron: 创建虚拟网卡
    Neutron-->>Comp: 返回端口信息
    Comp->>Cind: 挂载云硬盘(如需要)
    Cind-->>Comp: 挂载成功
    Comp->>Lib: virDomainCreate (创建VM)
    Lib-->>Comp: VM创建成功
    Comp-->>API: RPC返回VM状态
    API-->>User: 返回VM信息
```

## 三、OpenStack 部署架构

### 单节点部署

```mermaid
graph TB
    subgraph "单节点 OpenStack（All-in-One）"
        direction TB
        AllNode["单台物理服务器<br/>→ 运行所有OpenStack服务<br/>→ 同时作为计算节点和控制节点"]
    end

    subgraph "包含组件"
        direction TB
        API[Nova API]
        Sch[Nova Scheduler]
        Comp[Nova Compute]
        Neut[Neutron]
        Cind[Cinder]
        Glan[Glance]
        Swift[Swift]
        Keyst[Keystone]
        Hor[Horizon]
        DB[MySQL]
        MQ[RabbitMQ]
    end

    AllNode --> API
    AllNode --> Sch
    AllNode --> Comp
    AllNode --> Neut
    AllNode --> Cind
    AllNode --> Glan
    AllNode --> Swift
    AllNode --> Keyst
    AllNode --> Hor
    AllNode --> DB
    AllNode --> MQ

    style AllNode fill:#FF5722,color:#fff
```

| 特点 | 说明 |
|-----|------|
| **优点** | 部署简单、资源需求低、快速验证 |
| **缺点** | 无高可用、性能受限、无法生产使用 |
| **适用场景** | 学习、测试、POC 验证 |

### 多节点部署

```mermaid
graph TB
    subgraph "控制节点（Controller Node）"
        direction TB
        API["Nova API"]
        Sch["Nova Scheduler"]
        Con["Nova Conductor"]
        NeutAPI["Neutron Server"]
        NeutAgent["Neutron Agent"]
        CindAPI["Cinder API"]
        Glan["Glance API"]
        Keyst["Keystone"]
        Hor["Horizon"]
        Heat["Heat"]
        DB["MariaDB/Galera<br/>集群"]
        MQ["RabbitMQ<br/>集群"]
        Memcached["Memcached"]
    end

    subgraph "计算节点（Compute Node 1..N）"
        direction TB
        Comp["nova-compute"]
        NeutComp["neutron-agent<br/>OVS/ML2"]
        Stor["本地存储<br/>VM实例存储"]
    end

    subgraph "存储节点（Storage Node）"
        direction TB
        CindVol["cinder-volume"]
        Swift["Swift 对象存储"]
        Ceph["Ceph 集群<br/>可选后端"]
    end

    API --> Con
    Sch --> Con
    Con --> DB
    NeutAPI --> NeutAgent
    CindAPI --> CindVol
    Comp --> NeutComp
    Comp --> Stor
    Comp --> Neut
    Neut --> NeutComp
    Comp --> Cind
    Cind --> CindVol
    Cind --> Swift

    style API fill:#FF5722,color:#fff
    style Comp fill:#4CAF50,color:#fff
    style CindVol fill:#FF9800,color:#fff
```

| 特点 | 说明 |
|-----|------|
| **优点** | 高可用、性能可扩展、生产就绪 |
| **缺点** | 部署复杂、资源需求高 |
| **适用场景** | 企业生产环境、多租户云平台 |

### 部署工具

| 工具 | 特点 |
|-----|------|
| **DevStack** | 开发测试，快速部署单节点 |
| **Packstack** | 基于 Puppet 的快速部署 |
| **TripleO** | 基于 Heat 的完整部署方案 |
| **Kolla-ansible** | 容器化 OpenStack 部署 |
| **MicroStack** | Canonical 的轻量级方案 |

## 四、OpenStack 与其他私有云方案对比

### 主流私有云方案

```mermaid
graph TB
    subgraph "开源私有云"
        direction TB
        OS["OpenStack<br/>→ 开源IaaS<br/>→ 多组件架构<br/>→ 灵活可定制"]
        CM["CloudStack<br/>→ 开源IaaS<br/>→ 单进程架构<br/>→ 简单易用"]
        OP["OpenShift<br/>→ 基于K8s<br/>→ PaaS平台<br/>→ 容器原生"]
    end

    subgraph "商业私有云"
        direction TB
        VMware["VMware vSphere<br/>→ 商业虚拟化<br/>→ vCenter统一管理<br/>→ 成熟稳定"]
        HyperV["Microsoft Hyper-V<br/>→ Windows生态<br/>→ System Center管理"]
        Aliyun["阿里云专有云<br/>→ 公共云技术栈<br/>→ 本地化部署"]
    end

    subgraph "新兴方案"
        direction TB
        KubeVirt["KubeVirt<br/>→ K8s上运行VM<br/>→ VM + 容器统一"]
        Nutanix["Nutanix<br/>→ 超融合基础设施"]
    end

    style OS fill:#FF5722,color:#fff
    style VMware fill:#00BCD4,color:#fff
    style OP fill:#2196F3,color:#fff
    style KubeVirt fill:#9C27B0,color:#fff
```

### 对比表

| 维度 | OpenStack | CloudStack | VMware vSphere | OpenShift |
|-----|-----------|------------|----------------|-----------|
| **类型** | IaaS | IaaS | 商业IaaS | PaaS |
| **架构** | 多组件RPC | 单体Java | 单体C++ | K8s微服务 |
| **虚拟化后端** | KVM/QEMU等 | KVM/VMware | ESXi | KVM/KubeVirt |
| **API** | RESTful | RESTful | vCenter API | Kubernetes API |
| **网络** | Neutron SDN | 内置SDN | NSX | OpenShift SDN |
| **对象存储** | Swift | 内置 | - | - |
| **社区活跃度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | N/A | ⭐⭐⭐⭐ |
| **学习曲线** | 陡峭 | 中等 | 平缓 | 陡峭 |
| **适用规模** | 大中 | 中小 | 大中 | 大中 |
| **典型用户** | 运营商/大型企业 | 中小企业 | 中小企业 | 开发者/DevOps |

## 五、小结

> [!summary] 本节要点回顾
> 1. **云架构分层**：IaaS→PaaS→SaaS 三层模型，逐层抽象，各有定位
> 2. **OpenStack 核心组件**：Nova(计算)、Neutron(网络)、Cinder(块存储)、Glance(镜像)、Swift(对象存储)、Keystone(认证)、Horizon(界面)
> 3. **Nova 架构**：API→Scheduler→Compute→Conductor 的多层 RPC 架构
> 4. **部署模式**：单节点 All-in-One 适合测试，多节点适合生产
> 5. **私有云对比**：OpenStack 最灵活，VMware 最成熟，OpenShift 面向容器

## 关联知识点

| 关联知识点 | 关系 |
|-----------|------|
| [[4.2 资源虚拟化与调度算法]] | OpenStack 调度器详细解析 |
| [[4.3 云资源自动化管理]] | OpenStack IaC 编排 |
| [[5.1 云存储体系架构]] | OpenStack Cinder/Swift 存储详解 |
| [[第10章 主流云平台与云原生技术]] | OpenShift 与 OpenStack 对比 |