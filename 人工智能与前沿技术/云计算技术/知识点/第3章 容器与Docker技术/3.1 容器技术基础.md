---
domain: 人工智能与前沿技术
type: 概念
status: 整理中
created: 2026-07-29
course: 云计算技术
chapter: 3
section: 1
tags: ["云计算","虚拟化","容器Docker","云原生","负载均衡","云安全","期末复习"]
prerequisites: ["编程与算法/Python程序设计","人工智能与前沿技术/大数据分析技术","计算机系统/计算机网络"]
source: 《云计算技术与应用》本科教材、阿里云官方文档、云计算运维讲义、高校前沿技术课程讲义
---

# 3.1 容器技术基础

> [!abstract] 本节导读
> 容器技术是云原生时代的基石。本节从容器与虚拟机的深度对比切入，系统介绍 Linux 内核的两大核心机制——命名空间（Namespace）与控制组（cgroups），阐述容器的隔离原理与 OCI 标准，最后介绍 runc、containerd 等主流容器运行时。

## 一、容器 vs 虚拟机

### 架构对比

```mermaid
graph TB
    subgraph "虚拟机架构"
        direction TB
        App_VM["应用"]
        GOS["Guest OS<br/>独立内核"]
        HV["Hypervisor"]
        H_OS1["宿主OS"]
        HW1["物理硬件"]
        
        App_VM --> GOS
        GOS --> HV
        HV --> H_OS1
        H_OS1 --> HW1
    end

    subgraph "容器架构"
        direction TB
        App_C1["应用A"]
        App_C2["应用B"]
        Container1["容器1<br/>（进程隔离）"]
        Container2["容器2<br/>（进程隔离）"]
        H_OS2["宿主OS<br/>（共享内核）"]
        HW2["物理硬件"]
        
        App_C1 --> Container1
        App_C2 --> Container2
        Container1 & Container2 --> H_OS2
        H_OS2 --> HW2
    end

    style GOS fill:#FF9800,color:#fff
    style HV fill:#2196F3,color:#fff
    style H_OS1 fill:#9C27B0,color:#fff
    style Container1 fill:#4CAF50,color:#fff
    style Container2 fill:#4CAF50,color:#fff
    style H_OS2 fill:#2196F3,color:#fff
```

### 核心区别

| 维度 | 虚拟机 | 容器 |
|-----|--------|------|
| **隔离级别** | 操作系统级（独立内核） | 进程级（共享内核） |
| **隔离机制** | Hypervisor + 硬件虚拟化 | Namespace + Cgroups |
| **启动速度** | 分钟级（1-5分钟） | 秒级（< 3秒） |
| **资源占用** | 重（数百MB~数GB） | 轻（数十~数百MB） |
| **资源密度** | ~10:1 | ~100:1 |
| **内核** | 每个VM独立内核 | 共享宿主内核 |
| **兼容性** | 任意操作系统 | 同内核操作系统 |
| **安全边界** | 强（硬件级） | 中（进程级） |
| **性能** | 接近原生（95-99%） | 几乎原生（99-100%） |
| **持久化** | 独立磁盘镜像 | 联合文件系统 |
| **迁移** | 热迁移（复杂） | 镜像迁移（简单） |
| **Docker支持** | 需要完整OS | 原生支持 |

### 对比图示

```mermaid
graph LR
    subgraph "虚拟机"
        direction LR
        VM_OS["完整Guest OS<br/>+ Hypervisor<br/>+ Host OS"]
    end

    subgraph "容器"
        direction LR
        C_OS["仅应用层<br/>+ 共享Host OS内核"]
    end

    VM_OS -->|"资源占用<br/>10x - 100x"| C_OS

    style VM_OS fill:#2196F3,color:#fff
    style C_OS fill:#4CAF50,color:#fff
```

## 二、Linux 命名空间（Namespace）

### 命名空间概述

**命名空间（Namespace）** 是 Linux 内核提供的一种资源隔离机制。每个命名空间中的进程看到的是该命名空间的"视图"，无法感知或操作其他命名空间的资源。

> [!important] 命名空间的核心思想
> 通过为进程创建独立的资源视图，实现进程间的**资源隔离**。每个命名空间中的资源对该命名空间内的进程来说是"独占"的。

### 六大命名空间

```mermaid
mindmap
  root((Linux命名空间))
    PID命名空间
      独立进程ID
      进程树隔离
      容器内PID从1开始
    Network命名空间
      独立网络接口
      独立IP/路由表
      独立iptables规则
    Mount命名空间
      独立挂载点
      根文件系统隔离
      进程看不到宿主的挂载
    UTS命名空间
      独立hostname
      独立domainname
      容器内的主机名
    IPC命名空间
      独立消息队列
      独立信号量
      独立共享内存段
    User命名空间
      独立用户/组ID映射
      容器内root映射到宿主普通用户
      安全隔离增强
```

### 各命名空间详解

#### PID 命名空间

```mermaid
graph TB
    subgraph "宿主进程树"
        direction TB
        PID1["PID 1 (init/systemd)"]
        PID100["PID 100 (容器进程)"]
        PID200["PID 200 (其他进程)"]
    end

    subgraph "容器内视图"
        direction TB
        C_PID1["PID 1 (容器内init)"]
        C_PID2["PID 2 (应用进程)"]
        C_PID3["PID 3 (子进程)"]
    end

    PID100 -->|"在容器PID命名空间中<br/>PID被重新编号"| C_PID1

    style PID1 fill:#F44336,color:#fff
    style PID100 fill:#FF9800,color:#fff
    style C_PID1 fill:#4CAF50,color:#fff
    style C_PID2 fill:#4CAF50,color:#fff
    style C_PID3 fill:#4CAF50,color:#fff
```

**特点**：
- 容器内的进程有独立的 PID 编号空间
- 容器内的 PID 1 对应宿主中的某个进程
- 容器内无法看到或发送信号到其他命名空间的进程

#### Network 命名空间

```mermaid
graph TB
    subgraph "宿主网络"
        direction TB
        Eth0["eth0<br/>192.168.1.100"]
        Docker0["docker0<br/>172.17.0.1"]
        Routing["路由表"]
    end

    subgraph "容器1网络命名空间"
        direction TB
        Veth1["veth0<br/>172.17.0.2"]
        Loopback1["lo<br/>127.0.0.1"]
        Routing1["独立路由表"]
    end

    subgraph "容器2网络命名空间"
        direction TB
        Veth2["veth0<br/>172.17.0.3"]
        Loopback2["lo<br/>127.0.0.1"]
        Routing2["独立路由表"]
    end

    Docker0 -->|"虚拟网桥"| Veth1
    Docker0 -->|"虚拟网桥"| Veth2

    style Eth0 fill:#F44336,color:#fff
    style Docker0 fill:#2196F3,color:#fff
    style Veth1 fill:#4CAF50,color:#fff
    style Veth2 fill:#4CAF50,color:#fff
```

**特点**：
- 每个容器有独立的网络接口、IP 地址、路由表
- 通过 veth pair 连接到虚拟网桥（Docker 默认 docker0）
- 容器内只能看到自己的网络接口
- 支持多种网络模式：bridge、host、overlay、none

#### Mount 命名空间

**特点**：
- 容器有独立的根文件系统视图
- 容器内的进程无法看到宿主的挂载点
- 每个容器可以有不同的根文件系统
- 支持联合挂载（UnionFS）实现镜像分层

#### UTS 命名空间

**特点**：
- 容器有独立的 hostname 和 domainname
- `uname -n` 在容器内返回容器自己的主机名

#### IPC 命名空间

**特点**：
- 容器有独立的 IPC 资源（消息队列、信号量、共享内存）
- 容器间的 IPC 资源完全隔离

#### User 命名空间

**特点**：
- 容器内的用户 ID 与宿主映射
- 容器内的 root（UID 0）可以映射到宿主的普通用户
- 增强了容器的安全性

### 命名空间使用示例

```bash
# 创建 PID 命名空间
sudo unshare --fork --pid --mount-proc bash

# 创建 Network 命名空间
sudo ip netns add mynet
sudo ip netns exec mynet bash

# 在 Docker 中查看命名空间
docker run --rm -it --pid=host ubuntu bash  # 使用宿主PID命名空间
```

## 三、控制组（cgroups）

### cgroups 概述

**控制组（Control Groups, cgroups）** 是 Linux 内核提供的另一个资源隔离机制，用于**限制、统计和分配**进程组的系统资源（CPU、内存、I/O 等）。

> [!important] 命名空间 vs cgroups
> - **命名空间（Namespace）**：实现资源的"视图隔离"——进程看到的是独立的资源视图
> - **控制组（cgroups）**：实现资源的"数量限制"——进程能使用的资源有上限

### cgroups 核心功能

| 功能 | 说明 |
|-----|------|
| **资源限制** | 限制进程组的 CPU 时间、内存使用量、磁盘 I/O 等 |
| **优先级分配** | 为不同进程组分配不同的 CPU 时间片权重 |
| **资源统计** | 统计进程组的资源使用情况（CPU 时间、内存用量等） |
| **进程控制** | 挂起、恢复、杀死进程组中的所有进程 |

### cgroups 子系统

```mermaid
mindmap
  root((cgroups子系统))
    cpu
      限制CPU使用率
      设置CPU时间片权重
      实时调度
    cpuset
      绑定进程到特定CPU核心
      限制NUMA节点
    memory
      限制内存使用上限
      限制内存+交换分区
      内存使用统计
    blkio
      限制块设备I/O速度
      I/O优先级
      I/O统计
    devices
      控制设备访问权限
      允许/禁止特定设备
    freezer
      冻结进程
      恢复进程
    net_cls
      按流量分类
      网络带宽限制
    hugetlb
      限制大页内存使用
    pids
      限制进程数量
```

### cgroups v1 vs v2

| 维度 | cgroups v1 | cgroups v2 |
|-----|-----------|-----------|
| **挂载方式** | 每个子系统独立挂载 | 统一挂载（cgroup2fs） |
| **层级结构** | 每个控制器独立树 | 统一层级树 |
| **控制器** | 多个子系统 | 统一控制器接口 |
| **灵活性** | 较高（可独立配置） | 更高（统一管理） |
| **安全性** | 较弱 | 更强（支持User namespace） |
| **推荐状态** | 旧版兼容 | 推荐使用 |

### cgroups 使用示例

```bash
# 创建 cgroup
mkdir /sys/fs/cgroup/cpu/mycontainer

# 限制 CPU 使用率（相对于一个CPU核心）
echo 50000 > /sys/fs/cgroup/cpu/mycontainer/cpu.cfs_quota_us
echo 100000 > /sys/fs/cgroup/cpu/mycontainer/cpu.cfs_period_us

# 限制内存使用量
mkdir /sys/fs/cgroup/memory/mycontainer
echo 256M > /sys/fs/cgroup/memory/mycontainer/memory.limit_in_bytes

# 将进程加入 cgroup
echo 1234 > /sys/fs/cgroup/cpu/mycontainer/tasks
echo 1234 > /sys/fs/cgroup/memory/mycontainer/tasks
```

### Docker 与 cgroups 集成

```mermaid
graph TB
    subgraph "Docker 容器"
        direction TB
        C1["容器1<br/>cpu=50%<br/>memory=256M"]
        C2["容器2<br/>cpu=80%<br/>memory=512M"]
        C3["容器3<br/>cpu=20%<br/>memory=128M"]
    end

    subgraph "cgroups 层级"
        direction TB
        DockerCG["/docker<br/>（Docker根cgroup）"]
        C1CG["/docker/container1<br/>cpu.cfs_quota=50000<br/>memory.limit=256M"]
        C2CG["/docker/container2<br/>cpu.cfs_quota=80000<br/>memory.limit=512M"]
        C3CG["/docker/container3<br/>cpu.cfs_quota=20000<br/>memory.limit=128M"]
    end

    C1 --> C1CG
    C2 --> C2CG
    C3 --> C3CG
    C1CG & C2CG & C3CG --> DockerCG

    style DockerCG fill:#2196F3,color:#fff
    style C1CG fill:#4CAF50,color:#fff
    style C2CG fill:#4CAF50,color:#fff
    style C3CG fill:#4CAF50,color:#fff
```

## 四、容器隔离原理

### 隔离全景图

```mermaid
graph TB
    subgraph "容器隔离体系"
        direction TB
        A["隔离层 1：进程隔离<br/>→ PID/UTS/IPC命名空间"]
        B["隔离层 2：资源隔离<br/>→ cgroups资源限制"]
        C["隔离层 3：文件系统隔离<br/>→ Mount命名空间+UnionFS"]
        D["隔离层 4：网络隔离<br/>→ Network命名空间+虚拟网桥"]
        E["隔离层 5：安全隔离<br/>→ User命名空间+seccomp+AppArmor"]
    end

    subgraph "实现机制"
        direction TB
        A --> PID["PID Namespace<br/>→ 独立进程树"]
        A --> UTS["UTS Namespace<br/>→ 独立主机名"]
        A --> IPC["IPC Namespace<br/>→ 独立IPC资源"]
        B --> CG["Cgroups<br/>→ 资源使用上限"]
        C --> MNT["Mount Namespace<br/>→ 独立根文件系统"]
        C --> UFS["UnionFS<br/>→ 镜像分层"]
        D --> NET["Network Namespace<br/>→ 独立网络栈"]
        E --> USER["User Namespace<br/>→ UID映射"]
        E --> SEC["seccomp/AppArmor<br/>→ 系统调用过滤"]
    end

    style A fill:#4CAF50,color:#fff
    style B fill:#2196F3,color:#fff
    style C fill:#FF9800,color:#fff
    style D fill:#9C27B0,color:#fff
    style E fill:#F44336,color:#fff
```

### 隔离机制协同工作

```mermaid
sequenceDiagram
    participant Process as 容器内进程
    participant NS as 命名空间
    participant CG as cgroups
    participant Kernel as Linux内核

    Process->>Kernel: 系统调用
    Kernel->>NS: 查找进程所属的命名空间
    Note over NS: PID NS → 映射PID<br/>Net NS → 路由网络<br/>MNT NS → 解析路径
    Kernel->>CG: 检查cgroups资源限制
    Note over CG: CPU → 检查时间配额<br/>Memory → 检查内存上限<br/>BlkIO → 检查IO配额
    CG-->>Kernel: 资源允许
    Kernel-->>Process: 执行系统调用

    alt 超出资源限制
        CG-->>Kernel: 资源不足
        Kernel-->>Process: 返回错误/触发OOM
    end
```

## 五、OCI（Open Container Initiative）标准

### OCI 概述

**OCI（开放容器倡议）** 是由 Linux 基金会在 2015 年发起的开源组织，旨在制定容器技术的开放标准。

### OCI 标准组成

| 标准 | 说明 | 代表实现 |
|-----|------|---------|
| **OCI Runtime Specification** | 容器运行时规范，定义容器的生命周期 | runc、crun、Kata Containers |
| **OCI Image Specification** | 容器镜像规范，定义镜像的格式和内容 | Docker、containerd、CRI-O |
| **OCI Distribution Specification** | 容器镜像分发规范，定义镜像仓库协议 | Docker Registry、Harbor |

### OCI 生态

```mermaid
graph TB
    subgraph "OCI 标准"
        Runtime["Runtime Spec<br/>容器运行时规范"]
        Image["Image Spec<br/>镜像规范"]
        Distribution["Distribution Spec<br/>分发规范"]
    end

    subgraph "实现"
        direction TB
        runc["runc<br/>→ 参考实现"]
        crun["crun<br/>→ C语言实现"]
        containerd["containerd<br/>→ 容器管理"]
        Docker["Docker<br/>→ 完整容器平台"]
        Harbor["Harbor<br/>→ 企业级镜像仓库"]
    end

    Runtime --> runc
    Runtime --> crun
    Image --> containerd
    Image --> Docker
    Distribution --> Harbor

    style Runtime fill:#4CAF50,color:#fff
    style Image fill:#2196F3,color:#fff
    style Distribution fill:#FF9800,color:#fff
```

## 六、容器运行时

### 容器运行时架构

```mermaid
graph TB
    subgraph "容器编排层"
        direction LR
        K8s["Kubernetes"]
        Docker_CLI["Docker CLI"]
        Podman["Podman"]
    end

    subgraph "容器引擎层"
        direction LR
        DockerD["Docker Daemon"]
        ContainerD["containerd"]
        CRI_O["CRI-O"]
    end

    subgraph "容器运行时层"
        direction LR
        runc["runc"]
        crun["crun"]
        Kata["Kata Containers"]
    end

    subgraph "内核层"
        direction TB
        NS["Namespaces"]
        CG["Cgroups"]
        SECC["Seccomp/AppArmor"]
    end

    K8s --> CRI_O
    K8s --> ContainerD
    Docker_CLI --> DockerD
    Podman --> runc
    
    DockerD --> ContainerD
    ContainerD --> runc
    ContainerD --> crun
    CRI_O --> runc
    DockerD --> runc
    runc --> NS & CG & SECC
    crun --> NS & CG & SECC
    Kata --> NS & CG & SECC

    style K8s fill:#2196F3,color:#fff
    style DockerD fill:#4CAF50,color:#fff
    style ContainerD fill:#FF9800,color:#fff
    style runc fill:#9C27B0,color:#fff
```

### 主流容器运行时

| 运行时 | 语言 | 特点 | 适用场景 |
|-------|------|------|---------|
| **runc** | Go | OCI 参考实现，轻量、稳定 | Docker、containerd 底层 |
| **crun** | C | 轻量级，启动更快，内存占用更小 | 资源受限环境、边缘计算 |
| **Kata Containers** | Go/Rust | 基于虚拟化的轻量级容器 | 强隔离需求场景 |
| **gVisor** | Go | Google 开发，用户态内核 | 沙箱隔离、安全敏感场景 |
| **Firecracker** | Rust | AWS 开发，基于 KVM 的微虚拟机 | Serverless、函数计算 |

### containerd 架构

**containerd** 是一个工业级的容器运行时，自 Docker 1.11 开始从中拆分出来，专注于容器的运行和管理。

| 组件 | 说明 |
|-----|------|
| **containerd** | 守护进程，管理容器的完整生命周期 |
| **containerd-shim** | 为每个容器创建一个 shim 进程，确保容器独立运行 |
| **ctr** | containerd 的命令行工具 |
| **content store** | 镜像内容存储 |
| **snapshot store** | 容器快照存储 |

### Docker → containerd → runc 调用链

```mermaid
sequenceDiagram
    participant User as 用户
    participant Docker_CLI as docker CLI
    participant DockerD as dockerd
    participant ContainerD as containerd
    participant Shim as containerd-shim
    participant Runc as runc
    participant Kernel as Linux内核

    User->>Docker_CLI: docker run image
    Docker_CLI->>DockerD: POST /containers/create
    DockerD->>ContainerD: CreateContainer
    ContainerD->>Shim: 创建shim进程
    Shim->>Runc: runc create container.json
    Runc->>Kernel: 创建namespace + cgroups
    Kernel-->>Runc: 容器环境就绪
    Runc-->>Shim: 容器创建完成
    Shim-->>ContainerD: 容器已创建
    ContainerD-->>DockerD: 容器已创建
    DockerD-->>Docker_CLI: 返回容器ID
    Docker_CLI-->>User: 容器运行中
```

## 七、小结

> [!summary] 本节要点回顾
> 1. **容器vs虚拟机**：容器共享内核、轻量、快速启动；虚拟机独立内核、强隔离、较重
> 2. **命名空间**：PID、Network、Mount、UTS、IPC、User 六大命名空间实现资源视图隔离
> 3. **cgroups**：CPU、内存、blkio 等子系统实现资源使用量限制
> 4. **隔离原理**：命名空间隔离视图 + cgroups 限制资源 + seccomp/AppArmor 安全加固
> 5. **OCI标准**：Runtime、Image、Distribution 三大标准确保生态兼容
> 6. **容器运行时**：Docker → containerd → runc 三层架构，runc 是 OCI 参考实现

## 关联知识点

| 关联知识点 | 关系 |
|-----------|------|
| [[2.1 虚拟化概述与分类]] | 容器是操作系统级虚拟化 |
| [[2.2 Hypervisor与虚拟化架构]] | 容器vs虚拟机的架构对比 |
| [[3.2 Docker核心架构]] | Docker基于本节的容器技术实现 |
| [[10. 主流云平台与云原生技术]] | Kubernetes容器编排 |